/**
 * Centralized route constants for the application.
 * Use these instead of hardcoding route strings.
 */

export const ROUTES = {
  // ============================================
  // Core App Routes
  // ============================================
  HOME: "/",
  LOGIN: "/login",
  SETTINGS: "/settings",
} as const;
